apiVersion: v1
kind: Service
metadata:
  name: {{ include "plane-proxy.fullname" . }}
  labels:
    {{- include "plane-proxy.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "plane-proxy.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 80
      targetPort: 80
